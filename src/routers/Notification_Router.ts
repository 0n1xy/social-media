import { Router } from "express";
// import { getFollowRequests } from "@/controllers/Notification_Controller";

const router = Router();

// router.get("/follow/:id", getFollowRequests);

export default router;
